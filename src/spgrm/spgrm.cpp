#include "spgrm.h"
#include <iostream>

void createSparseGRM(const Parameters& params) {
    std::cout << "Creating sparse GRM with input: " << params.inputFile << " and output: " << params.outputFile << std::endl;
    // Implement the function
}
