#include "glmm.h"
#include <iostream>

void fitNullGLMM(const Parameters& params) {
    std::cout << "Fitting null GLMM with input: " << params.inputFile << " and output: " << params.outputFile << std::endl;
    // Implement the function
}
